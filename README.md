```bash
Step 1: Sync with main
git checkout main
git pull origin main

Step 2: Create a feature branch
git checkout -b user/task

Step 3: Do only their assigned task
No refactors, no extras.

Step 4: Push & open PR
git push origin user/task

PR → base: main
